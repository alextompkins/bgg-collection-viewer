[dev]
  command = "npm run dev:client"

[build]
  command = "npm run build"
  functions = "netlify/functions"
  publish = "dist"

[functions]
  node_bundler = "esbuild"

# SPA redirect
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
